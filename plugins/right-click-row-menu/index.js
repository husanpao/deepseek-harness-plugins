/**
 * Host half of the right-click row menu bundle.
 *
 * The bundle must mount a Host plugin so its Loader row activates, but none of
 * the behavior belongs on the Host: the row menu is rendered by the client-side
 * Workspace browser, so the whole feature lives in `./client`.
 */
export function apply() {}
